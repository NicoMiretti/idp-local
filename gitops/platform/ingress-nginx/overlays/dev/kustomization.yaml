# kustomization.yaml — ingress-nginx overlay dev
# Referencia el base/ sin parches adicionales para el entorno local.
# Se puede agregar patches: aquí para sobreescribir valores por ambiente.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
