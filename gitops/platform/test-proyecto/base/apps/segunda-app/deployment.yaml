apiVersion: apps/v1
kind: Deployment
metadata:
  name: segunda-app
  namespace: test-proyecto
  labels:
    app.kubernetes.io/name: segunda-app
    app.kubernetes.io/part-of: test-proyecto
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: segunda-app
  template:
    metadata:
      labels:
        app.kubernetes.io/name: segunda-app
        app.kubernetes.io/part-of: test-proyecto
    spec:
      containers:
        - name: segunda-app
          # Imagen que se actualizará via CI/CD cuando se haga push al repo de código.
          # registry.local/<project>/<app>:latest apunta al registro interno de Kind.
          image: registry.local:5000/test-proyecto/segunda-app:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: segunda-app-config
            - secretRef:
                name: test-proyecto-secret
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
