# kustomization.yaml — test-proyecto apps/mi-primera-app
# Recursos de la aplicación mi-primera-app dentro del proyecto test-proyecto.
# Generado automáticamente por Backstage Scaffolder.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: test-proyecto

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml
  - secret.yaml
